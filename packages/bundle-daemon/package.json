{
  "name": "@dsh-remote/bundle-daemon",
  "version": "0.0.0",
  "private": true,
  "description": "dsh profile bundle: daemon mode frontend — attach/detach sessions on a remote headless dsh over SSH (ctx.remoteHub + ctx.remoteSessions + transfer/monitor)",
  "type": "module",
  "files": [
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@dsh-remote/remote-daemon": "workspace:*",
    "@dsh-remote/remote-frontend": "workspace:*",
    "@dsh-remote/remote-ssh": "workspace:*"
  }
}
