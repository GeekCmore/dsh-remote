{
  "name": "@dsh-remote/bundle-live",
  "version": "0.0.0",
  "private": true,
  "description": "dsh profile bundle: live mode — route ctx.fs / ctx.subprocess of a local dsh session to a remote Linux host over SSH (agentless)",
  "type": "module",
  "files": [
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@dsh-remote/fs-ssh": "workspace:*",
    "@dsh-remote/remote-ssh": "workspace:*",
    "@dsh-remote/subprocess-ssh": "workspace:*"
  }
}
