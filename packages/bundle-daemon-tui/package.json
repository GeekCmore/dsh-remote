{
  "name": "@dsh-remote/bundle-daemon-tui",
  "version": "0.0.0",
  "private": true,
  "description": "dsh profile bundle: daemon mode for in-process TUI frontends — remote-backed session seams (sessions/agents/sessionPersistence) so seam-compliant TUIs attach to a remote headless dsh unmodified",
  "type": "module",
  "files": [
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@dsh-remote/proxy": "workspace:*",
    "@dsh-remote/remote-daemon": "workspace:*",
    "@dsh-remote/remote-ssh": "workspace:*"
  }
}
